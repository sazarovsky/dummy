name: Python package
run-name: "#${{github.run_number}} : ${{github.event.inputs.MAIN_CODE_BRANCH}}"

on:
  push:
  workflow_dispatch:
    inputs:
      MAIN_CODE_BRANCH:
        description: "The main code branch."
        default: 'LALALA'
        required: true
        type: string

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          echo lalala
      - name: Printing action parameters
        run: |
          #!Printing action parameters
          echo ACTIONS_BRANCH: ${{ github.action }}#%ACTIONS_BRANCH%
          echo github.action_path: ${{ github.action_path }}
          echo github.action_ref: ${{ github.action_ref }}
          echo github.action_repository: ${{ github.action_repository }}
          echo github.actor: ${{ github.actor }}
          echo github.ref_name: ${{ github.ref_name }}
          echo github.action: ${{ github.action }}
          echo github.workflow: ${{ github.workflow }}
