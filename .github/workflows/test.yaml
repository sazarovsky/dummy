name: Python package
run-name: "#${{github.run_number}} : ${{ github.ref }}"

on:
  workflow_dispatch:
    inputs:
      INPUT_VAR:
        description: "dummy"
        default: false
        required: true
        type: boolean
  push:
    inputs:
      INPUT_VAR:
        description: "dummy"
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          echo github.event_name = ${{ github.event_name }}

      - name: Printing action parameters
        run: |
          #!Printing action parameters
          echo inputs.INPUT: ${{github.event.inputs.INPUT_VAR}}

      - name: Print the input name to STDOUT
        run: echo The username is ${{ env.INPUT_VAR }}
  test:
    needs: build
    if: ${{ github.event.inputs.INPUT_VAR == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - name: Install dependencies
        run: |
          echo lalala test
